import { Typography, Container } from "@mui/material";

function Header() {
  return (
    <Container>
      <Typography variant="h1">Sunday</Typography>
      <Typography variant="h2">13 August</Typography>
    </Container>
  );
}

export default Header;
